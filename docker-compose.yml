version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: cheapest-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-cheapest}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - cheapest-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cheapest-api
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      DB_TYPE: postgres
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: ${DB_USERNAME:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: ${DB_NAME:-cheapest}
      PORT: 3000
      LOAD_SEED_FILE: ${LOAD_SEED_FILE:-load-seed.yaml}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - cheapest-network
    command: >
      sh -c "sleep 5 && npm run start:prod"

volumes:
  postgres_data:
    driver: local

networks:
  cheapest-network:
    driver: bridge
